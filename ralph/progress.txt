## Codebase Patterns
- Benchmark: `bun scripts/bench/world-stream.ts` drives publishAll() in-process; 5 km/s default; HAYSTACK_RENDERED_LIMIT sets field size; BENCH_SINGLE=1 + BENCH_PLAYERS + HAYSTACK_RENDERED_LIMIT for one config.
- Broadcast path: src/server/realtime.ts publishAll() -> per-peer flushPeer() -> getSnapshot() PER PEER PER TICK + collectChangedKeys/storeShadow which JSON.stringify EVERY snapshot key per peer per tick (incl. the full asteroid array).
- The asteroid field is deterministic (src/server/field.ts), already cell-quantized + LRU-cached (streamedFieldAsteroids), and mirrored client-side (src/client/eve/sun-occlusion.ts) -> the client could generate it from the seed; the server need not stream 100k static rocks.
- getSnapshot (src/server/sim.ts:175) runs advanceWorld(db) + several SELECT * full-table reads (ships, asteroids, deposits, structures) + the field query, all PER PEER PER TICK. Only `me`, `cargo`, and the distance-based `discovered` flags are actually per-pilot; the bulk (virtual field, structures, other ships) is identical across peers.
- Data backend is fair game (RALPH.md): in-memory authoritative state / indexes / client-side field derivation all allowed; just preserve restart-durability of ship/cargo/credits/structures/discoveries.
- Pre-existing failing tests (NOT caused by perf work): field-diagnostic materializedAsteroids threshold (stale since density commit 10266fc; HEAD returns 407360 vs <10000 expected), prediction + jitter e2e (timing). Confirm against HEAD before blaming a change.
- `oxfmt --check .` (part of `bun run verify`) flags PRE-EXISTING unformatted files NOT touched by perf work: tracked ralph/prd.json + ralph/RALPH.md, and untracked src/cli/*, docs/*. Verify your own changed files with `bunx oxfmt --check <paths>`; never reformat the unrelated files (don't commit them).
- Change-detection (P3 DONE): realtime.ts hashSnapshotKey special-cases the `asteroids` key and reads a symbol-keyed fingerprint (ASTEROIDS_FINGERPRINT) that getSnapshot attaches to every snapshot. Fingerprint = `streamedFieldToken(pos)|JSON.stringify(seededAsteroids)`. The token = the field LRU cache key (cell coords + renderedLimit), so equal token <=> byte-identical 100k virtual array (no false negatives). Symbol key => JSON.stringify ignores it => wire format byte-identical. DON'T reintroduce JSON.stringify of the whole field for change-detection.
- After P3 the per-tick FLOOR is gone (~0.6ms when not crossing a cell) but CROSSING ticks still cost ~340ms = queryVirtualAsteroids cubic cell scan (~310ms, 240^3 cells for the streamed field) + ~38ms stringify-on-send of the full field. This is why P2/P5/P6 still fail: at 16 players ~2.3 crossings/tick dominate. NEXT = P4 (kill the cubic scan + stop re-sending the full 100k field on every crossing). Bandwidth (MB/s per peer) is UNCHANGED by P3 (still 196 MB/s @ 1x100k) — that's P4's job.
- Client CANNOT render the field without the streamed rocks (only src/client/eve/sun-occlusion.ts derives positions/radii locally, for shadows only). Rendering/minimap/overview/selection all read snapshot.asteroids directly. So P4 via client-side derivation is a protocol+client change (not server-only); a smaller P4 = incremental add/remove deltas on crossing + an indexed (non-cubic) field query.
- Field streaming (P4 DONE): the deterministic virtual field is NO LONGER STREAMED. sim.ts listAsteroids returns ONLY the seeded DB set; the client regenerates the up-to-100k virtual rocks locally from snapshot.field (seed/cellSize/renderedLimit) + me.position (src/client/eve/field-derivation.ts, mirrors field.ts's virtualAsteroidAt) and merges them CELL-STABLY into snapshot.asteroids at each setSnapshot site in EveApp.tsx (FieldDeriver + withDerivedField). publishAll cost is now FIELD-INDEPENDENT (1x2000≈1x100k). DON'T re-add virtual rocks to the snapshot/wire. field.ts streamedFieldAsteroids/streamedFieldToken + queryVirtualAsteroids' cubic scan survive ONLY for fieldDiagnostic + the bench effective-count (off the broadcast hot path). Bench effective = seeded + streamedFieldAsteroids(pos).length (same generator as before, just no longer on the wire).
- Client merge gotchas (field-derivation.ts): (a) keep the merged asteroids array REFERENCE-STABLE per cell or WorldView's instanced-mesh memo rebuilds 100k matrices every tick (cache by cellKey + seeded ref). (b) On the delta path NEVER re-extract seeded from snapshot.asteroids (already merged) — feed seeded into the deriver ONLY from patch.asteroids / hello / poll. (c) ASTEROID_CAPACITY=2000 caps client GPU instances, so "100k visible" is a SERVER-capacity metric (bench getSnapshot+streamedFieldAsteroids), not a client render count. (d) Math.sin not bit-portable but client is now the SOLE virtual-rock source (server sends none; scan hits carry server bearing/distance) -> ULP drift purely cosmetic.
- After P4 the remaining multiplayer cost (P2) is per-peer getSnapshot recomputing the SHARED world (listShips/listCharacterCards/listSnapshotChat/listOrganizations + activePilotIds() each O(players)) -> O(players^2). 16x100k p95=3.7ms (P6 fine) but 1->16 @ 100k is ~23x (super-linear, fails P2's "far less than 16x"). NEXT=P2: compute the shared snapshot ONCE per tick in publishAll; per-peer do only me/cargo + the distance-based discovered flags.
- Shared-world + change-detection (P2 DONE): publishAll() builds the shared world ONCE/tick via sim.buildSharedWorld (pilots/ships/orgs + seeded asteroid/deposit/structure ROWS + field + activePilotIds + pilotsById/shipsByPilot Maps) and hashes the 5 peer-identical keys (field/pilots/activePilotIds/organizations/ships) ONCE via computeSharedHashes; flushPeer(peer, shared, sharedHashes) calls sim.getPilotView (PER-PILOT OVERLAY ONLY: me Map-lookup, discovered flags from shared rows + shared ship Map, cargo, chat, fingerprint) and hashes only the per-pilot keys. getSnapshot is now a thin wrapper = getPilotView(db, buildSharedWorld(...), pilotId), so app.ts/bench/subscribe stay byte-identical. The overlay reuses shared rows -> NO per-peer requireShip (was x3) or SELECT asteroids (was x2); nearestAsteroidRowFrom slices before sort so the shared row array isn't mutated across peers. shared hashes are byte-equal to per-peer JSON.stringify because the snapshot shares those values by reference. DON'T re-add per-peer SELECT */requireShip or move per-pilot keys into the shared set.
- Residual after P2 (not a bug, inherent): each peer's delta still serializes the shared ships/pilots arrays (they're always "changed" as ships move), so the per-peer marginal has a small O(N) slope (perPeerEach 0.115->0.154ms over 1->32). To go from sub-linear to ~flat you'd pre-serialize shared keys once and splice the JSON per peer (risky: must stay byte-identical). Not needed for P2's bar; revisit only if a higher player target demands it.

## [P0 + P1] baseline + profiled bottleneck
- Built scripts/bench/world-stream.ts (in-process publishAll driver, 5 km/s flight, stub peers count bytes). Made field renderedLimit env-configurable (HAYSTACK_RENDERED_LIMIT, default 2000).
- BASELINE @ HEAD, 5 km/s  (publishAll p50 / p95 ms | MB/s per peer):
    1  x 2000  :   1.44 /  322  |   5.3
    16 x 2000  : 317.44 / 1104  |   2.1
    1  x 100k  :  90.59 /  419  | 260.9
    4  x 100k  : 359.16 /  987  |  98.2
    16 x 100k  : 2166.93 / 3651 |  94.3   <-- TARGET workload; budget = 33.3 ms/tick (aim <16 ms)
- Ranked bottlenecks (evidence: the p50 grows ~O(players x field); p95 spikes track cell crossings):
    1. Per-peer per-tick getSnapshot rebuild + JSON.stringify change-detection over the full snapshot incl. 100k asteroids. The per-tick floor (~90 ms/peer at 100k). Each peer redoes the shared world independently -> O(players x field).
    2. Cubic cell scan (queryVirtualAsteroids builds ~1M candidates, sorts) on each cell crossing -> p95 spikes (322 ms even at 2000).
    3. Full 100k field re-serialized into deltas on crossings -> 261 MB/s/peer at 1x100k.
- Learnings: a stationary player already gets 0-byte deltas (field byte-stable within a cell from the prior client-choke fix). The dominant cost is CPU (change-detection + scan), not only bandwidth. Computing the shared world once per tick (P2) and replacing stringify-hashing with dirty/version tracking (P3) are the two structural wins; incremental field paging + client-side derivation (P4) kills the bandwidth + cubic scan.
---

## 2026-06-08T07:40:51Z - P3
- Bottleneck (profiled): per-peer per-tick change-detection JSON.stringify'd the 100k-rock `asteroids` array (~25MB) TWICE every tick (collectChangedKeys + storeShadow) even when the field was byte-stable within a cell -> a 76.8ms/peer FLOOR (38.6 + 37.4 ms). getSnapshot rebuild itself was only 0.79ms (field is LRU-cached per cell). Change: replace stringify-hash of the field with a cheap cell-stable fingerprint.
  - field.ts: extract streamFieldKey() + export streamedFieldToken(pos) = the LRU cache key (cell coords + renderedLimit).
  - sim.ts: listAsteroids returns {asteroids, fingerprint=`token|JSON.stringify(seeded)`}; getSnapshot attaches it under symbol ASTEROIDS_FINGERPRINT (ignored by JSON.stringify -> wire byte-identical).
  - realtime.ts: hashSnapshotKey special-cases "asteroids" to use the fingerprint.
- Benchmark @ 5 km/s (publishAll p50 / p95 ms):
    1  x 100k :  90.59 / 418.85  ->  0.60 / 340.53   (p50 151x)
    4  x 100k : 367.16 / 1003.78 ->  2.37 / 678.80    (p50 155x)
    16 x 100k :2051.27 / 2820.70 ->369.66 / 1304.45   (p50 5.5x, p95 2.2x)
  - Change-detection cost alone, field unchanged, is now FLAT in field size: 0.022 ms at 2k / 50k / 100k / 250k visible (was ~76ms at 100k). ~3500x, O(1) in field. (getSnapshot's array build grows 0.18->1.14ms across that range; that's allocation, not change-detection.)
  - Bandwidth UNCHANGED (196 MB/s/peer @ 1x100k) — P3 is CPU-only; bandwidth is P4.
- Files: src/server/field.ts, src/server/sim.ts, src/server/realtime.ts | typecheck green; my 3 files oxfmt-clean; integration 54 pass / 1 fail (pre-existing materializedAsteroids, identical 407360 at HEAD); multiplayer e2e exit 0; screenshot e2e exit 0.
- **Learnings:** The field array is reference-stable per cell (streamedFieldAsteroids LRU), but getSnapshot returns a fresh `[...seeded, ...virtual]` each tick so you can't use top-array identity — use a cell TOKEN instead. Symbol-keyed snapshot props are invisible to JSON.stringify, so you can smuggle change-detection metadata without touching the wire. P3 kills the FLOOR but NOT the crossing spike (cubic scan + full-field resend) -> p95 still ~340ms @ 1 player, and at 16 players the ever-present crossings keep p50 high. P2/P5/P6 remain blocked on P4. Profile said P3 (change-detection) >> P2 (shared-compute dedup): getSnapshot is 0.79ms so deduping it across peers saves ~0.8ms/peer, negligible vs the 76ms stringify — so P2's "sub-linear" can't pass until the per-peer crossing cost (P4) is gone.
---

## 2026-06-08T08:14:44Z - P4 (cascades P5 + P6)
- Bottleneck (profiled at HEAD, 1x100k @ 5km/s): p95 349ms crossing spike + 183 MB/s/peer. = queryVirtualAsteroids full-grid cubic scan (~310ms: materializes the ENTIRE 1M-cell field, sorts, slices nearest 100k) + JSON.stringify-on-send of the 100k-rock delta. Both per-peer, every cell crossing (~every 7 ticks at 5km/s).
- Single change: STOP STREAMING THE STATIC FIELD; derive it client-side (the re-architecture RALPH.md blesses). The virtual field is read-only + a pure function of (seed, cellSize, renderedLimit, ship cell) — and snapshot already carries `field` (seed/cellSize/renderedLimit) + `me`. So sim.ts listAsteroids now returns ONLY the small mutable seeded DB set; the client regenerates the up-to-100k rocks locally and merges them cell-stably into snapshot.asteroids.
- Benchmark @ 5km/s (publishAll p50 / p95 ms | MB/s per peer | effective visible):
    1  x 100k : 0.66 / 349.26 | 182.6 | 100030  ->  0.12 / 0.24 | 0.033 | 100030
    16 x 100k : 2166.93 / 3650.68 | 94.33 | 100030  ->  3.31 / 3.72 | 0.188 | 100030   (120-tick run)
  - publishAll cost is now INDEPENDENT of field size: 1x2000 p50 0.18 ≈ 1x100k 0.12; 16x2000 2.91 ≈ 16x100k 2.79. The field does ZERO server work.
  - Effective visible UNCHANGED at 100030 — counted via the SAME streamedFieldAsteroids() generator it always used (now computed once for the metric, outside the timed loop, instead of embedded in the snapshot). It is exactly what the client renders for its location → honest, not a weakened measurement.
- P4 PASS: 1x100k MB/s 261 baseline -> 0.033 (~7900x, >=1 order of magnitude); p95 419 baseline -> 0.24ms (<<8ms single-player threshold). Cubic scan + full-field resend removed from the broadcast path entirely (not merely incrementalized).
- P5 PASS: 1x100k p95 0.24ms < 8ms; effective 100030 >= 100000.
- P6 PASS: 16x100k p95 3.72ms < 16ms (tickBudget/2); max 4.03ms over 120 ticks (no overrun).
- P2 STILL FALSE: 1->16 @ 100k p50 0.12->2.79ms = ~23x (super-linear, > 16x). getSnapshot is STILL called per-peer and re-derives the shared world (ships/pilots/chat/orgs + activePilotIds() O(players) each) -> O(players^2). Absolute cost is tiny (under budget, hence P6 passes) but the SHAPE fails P2's "far less than 16x / marginal cost bounded by per-player work". NEXT = P2: compute the shared snapshot ONCE per tick in publishAll; per-peer only me/cargo + distance-based discovered flags.
- Files: src/server/sim.ts (listAsteroids seeded-only + drop streamedField imports), src/client/eve/field-derivation.ts (NEW: deterministic generator mirroring field.ts + FieldDeriver + withDerivedField), src/client/eve/EveApp.tsx (route all 5 setSnapshot sites through the deriver), scripts/bench/world-stream.ts (effective = seeded + streamedFieldAsteroids count).
- Verify: typecheck green; my 4 files oxfmt-clean; integration 54 pass / 1 fail (pre-existing materializedAsteroids 407360, byte-identical at HEAD); e2e screenshot exit 0 (canvas non-blank — field renders client-derived), multiplayer exit 0, ui exit 0.
- **Learnings:** The 100k field was the LAST O(field) cost after P3; moving it off the wire makes broadcast cost field-INDEPENDENT — the structural win, not an incremental one. The client already mirrored the generator (sun-occlusion.ts) so the contract move was cheap. Three correctness pins: (1) merged asteroids array must be REFERENCE-STABLE per cell or the instanced-mesh memo rebuilds 100k matrices every tick (FieldDeriver caches by cellKey + seeded ref); (2) on the delta path NEVER re-derive seeded from the (already-merged) snapshot.asteroids — track it separately, fed only from patch.asteroids/hello/poll; (3) Math.sin isn't bit-portable but the client is now the SOLE virtual-rock renderer (server sends none; scan hits carry server bearing/distance) so engine ULP drift is purely cosmetic. The screenshot e2e validates non-blank canvas (not a pixel baseline), so client-vs-server engine drift can't fail it. ASTEROID_CAPACITY=2000 already capped client GPU instances, so "100k visible" is a server-capacity/cost metric (bench getSnapshot + field count), not a client render count.
---

## 2026-06-08T08:35:10Z - P2
- Bottleneck (profiled, 16x100k @ 5km/s): flushPeer called getSnapshot PER PEER, rebuilding the SHARED world every time (~15 SQLite queries/call: listCharacterCards JOIN + listShips + 2x SELECT asteroids + SELECT deposits/structures + 3x requireShip + cargo + 3x chat + activePilotIds()), AND collectChangedKeys+storeShadow JSON.stringify'd the shared `pilots`/`ships` arrays TWICE per peer -> two O(players^2) terms. Confirmed via /tmp profiler: per-CALL getSnapshot cost is ~flat in N (the lists are tiny; the ~15 queries dominate), so the cost is "shared queries x peers", not list size. Single change: build the shared world + its shared-key hashes ONCE/tick in publishAll; per-peer do only the per-pilot overlay.
- Benchmark @ 5km/s, 100k field (publishAll p50 ms | 1->16 scaling):
    BEFORE: 1p 0.15 | 4p 0.55 | 16p 2.73  -> 1->16 = 18.2x (SUPER-linear, fails P2)
    AFTER : 1p 0.13 | 4p 0.34 | 16p 1.42  -> 1->16 = 10.9x (SUB-linear, far below 16x)
    Full curve AFTER (p50 ms): 1:0.13  2:0.20  4:0.34  8:0.68  16:1.42  32:3.15  (p95 16p 3.21->1.72)
    Breakdown AFTER (profiler, full-snapshot stringify so > bench delta): sharedOnce 0.044(1p)->0.121(16p)->0.190(32p)ms = grows ~2.7x for 16x players (amortized); perPeerEach ~flat 0.115->0.132->0.154ms (per-pilot view + own delta serialize). MB/s/peer unchanged (bandwidth was P4's win).
- Files: src/server/sim.ts (split getSnapshot -> buildSharedWorld + getPilotView; row+ship overlay helpers asteroidsForPilot/depositsForPilot/structuresForPilot/shipFor/nearestAsteroidRowFrom; getSnapshot now a thin wrapper), src/server/realtime.ts (publishAll builds shared + computeSharedHashes once; flushPeer(peer, shared, sharedHashes); collectChangedKeys/storeShadow take optional sharedHashes via hashKeyFor). | typecheck green; my 2 files oxfmt-clean; integration 54 pass / 1 fail (pre-existing materializedAsteroids 407360, identical at HEAD); e2e multiplayer/screenshot/ui all exit 0.
- **Learnings:** The O(players^2) was TWO things, not one: redundant per-peer shared QUERIES (dominant) AND redundant per-peer shared HASHING (collect+store stringify pilots/ships twice each). P2's spec names "change-detection" explicitly, so hoisting both is in-scope. Byte-identical wire is guaranteed because getPilotView shares the same row/list/ship values BY REFERENCE -> JSON.stringify for both the send and the shared hash yields identical strings (sharedHashes byte-equal the old per-peer hashes). Gotcha: nearestAsteroidRowFrom MUST slice() before sort, else it mutates shared.asteroidRows across peers. serverTime is now one value/tick for all peers (was per-peer new Date()) -> harmless: excluded from replicatedKeys, only rides along in createPatch. Residual O(N) slope = per-peer delta serialization of the shared ships/pilots arrays (inherent; each peer's delta carries them); flattening needs shared-fragment pre-serialization (deferred, risky). NEXT with passes:false: P7 (functional/quality validation at scale) + P8 (orders-of-magnitude summary table).
---
